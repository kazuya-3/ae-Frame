# このリポジトリで作業するときは

**まず [`docs/HANDOFF.md`](docs/HANDOFF.md) を読んでください。** 触りかた・約束事・
落とし穴・いまの状態と次の一手が、そこにまとまっています。
「何を作ったか・なぜそう作ったか」は [`README.md`](README.md) にあります。

急ぎのときのために、要点だけ:

```bash
npm ci
npm run dev                                   # 開発サーバー
npm run typecheck                             # 型
npx prettier --write "src/**/*.{ts,tsx,css}"  # 整形（必須）
npm run build && npm test                     # 配るものを作って、実ブラウザで通し検証
node tools/check-dist.mjs                     # 配るものの中身（禁止語・重さ・残りかす）
```

検証は Chromium を起動します。場所は `CHROMIUM_PATH` で渡せます。
1回 8〜10分かかるので、最後の `全 N 件 合格` まで待ってください。

変えてはいけない約束（詳しくは HANDOFF の 3.）:

- 画像・動画は**端末から出さない**
- 見えている絵と、保存される絵を**1ピクセルも違わせない**
- 画面に、色の付いた大きなボタンを**2つ出さない**
- 専門用語を画面に出さない（保存は「形式名」ではなく「使う場所」で選ばせる）
- 本体 JS 95KB / CSS 12KB（gzip）の上限を超えない。スタジオは遅延読み込みのまま
- コメントは「なぜそうしたか」を日本語で書く
